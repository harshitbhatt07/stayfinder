import React from "react";
import{useEffect,useState}from'react';import api from '../api';import{imageUrl}from'../utils/media';
export default function MyBookings(){const[bookings,setBookings]=useState([]);const[notifications,setNotifications]=useState([]);const load=()=>{api.get('/bookings/my').then(r=>setBookings(r.data));api.get('/notifications').then(r=>setNotifications(r.data)).catch(()=>{})};useEffect(load,[]);const cancel=async(id)=>{await api.patch(`/bookings/${id}/cancel`);load()};return <div className="max-w-5xl mx-auto p-6"><h1 className="text-3xl font-bold mb-5">My Bookings</h1>{notifications.length>0&&<div className="mb-6"><h2 className="text-xl font-bold mb-3">Notifications</h2>{notifications.map(n=><div key={n._id} className={`p-3 rounded mb-2 ${n.type==='success'?'bg-green-50':'bg-blue-50'}`}><b>{n.title}</b><p>{n.message}</p></div>)}</div>}{bookings.map(b=><div className="bg-white p-5 rounded-xl shadow mb-4" key={b._id}><div className="grid md:grid-cols-[140px_1fr] gap-4"><img src={imageUrl(b.room?.images?.[0])} className="h-28 w-full object-cover rounded-xl"/><div><h2 className="font-bold">{b.room?.title}</h2><p>Owner: {b.owner?.name} | {b.owner?.phone}</p><p>Check-in: {b.checkIn?.slice(0,10)} | Check-out: {b.checkOut?.slice(0,10)}</p><p>Guests: {b.guests} | Rooms: {b.roomsCount} | Food: {b.foodRequired?'Yes':'No'}</p><p>Amount: ₹{b.amount}</p><p>Payment: {b.paymentMethod} | {b.paymentStatus}</p><p>Status: <b>{b.status}</b></p>{['pending','confirmed'].includes(b.status)&&<button onClick={()=>cancel(b._id)} className="text-red-600">Cancel</button>}</div></div></div>)}</div>}
